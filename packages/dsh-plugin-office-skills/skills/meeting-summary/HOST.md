# 会议总结：e-Mate 本机适配

此预置入口注册名为 `meeting-summary`，上游原名为 `meeting-transcript-summary`。
本 Skill 的绝对资源目录为：{{SKILL_DIR}}
文中相对路径均以该目录为基准；不要假设当前项目包含 Skill 脚本。

- 使用当前任务的原始转录文本或可读取附件。原文是事实和引用的权威来源，始终保留原件，不覆盖上传文件。
- 可直接阅读原文并按以下上游规范总结；清洗不是必需步骤。缺少 Python 不妨碍文本总结，也不需要安装依赖或连接外部会议服务。
- 如需辅助清洗，使用当前 e-Mate 运行环境中已提供且验证可执行的原生 Python，运行资源目录下 `scripts/clean_transcript.py`，输入、输出使用带引号的绝对路径，输出另存到当前任务工作区。脚本仅依赖 Python 标准库，不申请密钥、不上传转录。
- 清洗结果仅帮助阅读，不替代原文。日期、金额、独立数字、中文说话人、决策和行动项必须逐项与原文核对。原脚本会移除时间码；需要时间位置的出处必须回到原文。
- 不补写未提供的责任人或时间。音频转录不是此 Skill 的功能；只有音频时先使用当前任务中实际可用且已获授权的转录能力，不声称已听取或转录。
- 输出沿用当前 Agent 和文件/附件能力。默认给出 Markdown 会议纪要；用户明确要求交互看板时才使用原版模板，不自动生成额外文件。

以下保留上游原版操作内容。
