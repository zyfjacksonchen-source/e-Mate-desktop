# Spreadsheets Skill source and host boundary

- Source: OpenAI Codex primary runtime `spreadsheets` plugin, version `26.905.11957`, specifically its `skills/spreadsheets/` subtree. The sibling `excel-live-control` Skill and plugin app connector are not included.
- Original author metadata: OpenAI. `UPSTREAM_PLUGIN_MANIFEST.json` preserves the source plugin manifest, which declares MIT. Inspection of this Skill, references, plugin and cache ancestors found no separate more-specific LICENSE or redistribution restriction. This records the available declaration; it does not invent a missing license file or assert that separately licensed dependencies are covered.
- All 19 original Skill files are byte-for-byte unchanged, including SKILL.md, workflows, domain guidance, relative references, API reference documents, marker, icon and agents metadata. `UPSTREAM.json` lists SHA-256 and size for each, plus the original manifest digest. All relative Markdown links resolve within the original subtree.
- e-Mate adds only this record, `UPSTREAM.json`, the preserved manifest and separate `HOST.md`. The original instruction permits openpyxl/xlsxwriter/pandas.ExcelWriter authoring when artifact-tool is unavailable. The host note uses this existing exception, supplies `DSH_EMATE_PYTHON` and resource path guidance, and keeps factual formula/recalculation/rendering acceptance mandatory.
- `@oai/artifact-tool` 2.8.59 is explicitly excluded. Its separately inspected LICENSE.md identifies a proprietary runtime limited to internal evaluation/testing and prohibits production use or third-party distribution without written permission. Only its identity and license digest are recorded; no file from that runtime package is copied. Skill API examples are not an installed runtime or a compatibility implementation.
- The marker validates command-line arguments only. This source replacement did not execute it, install Python packages, create or modify a workbook, calculate formulas, render sheets, import Google Sheets, or run installed-client acceptance. Root integration must attach HOST.md and report actual dependency readiness and validation outcomes.
