/** Image-only tool guidance; never adds a planning/model round or rewrites admitted arguments. */
export const IMAGE_PROMPT_GUIDANCE = `Before preparing the parent request, distinguish a new image (including generation guided by references) from changing an existing image. An attached reference is not automatically an edit target. Inspect selected local images through the existing attachment/vision owner if they are not already visible; do not infer unseen contents or reopen visible references just for ceremony.
Write a concise instruction covering intended use, subject, style/composition and necessary constraints. Preserve detailed user requests instead of inventing extra objects, brands, slogans or arbitrary layout. Label every supplied image by its ordered role: edit target, identity/style/composition reference, or insert/compositing source. Explain their relationship. For edits and follow-ups, explicitly repeat what changes and what stays fixed, including identity, marks, framing and unedited regions. For removing text from an existing poster/photo, supply that exact image as image_url and describe a local text-only repair: remove only the requested lettering and reconstruct the background beneath it; preserve every person’s identity, facial features, expression, pose, clothing, framing and all other unedited regions. Do not reinterpret this as recreating the poster or beautifying faces. An edits route still does not guarantee unchanged identity: inspect the returned faces and composition against the source before claiming success. When the user asks to follow the reference ratio or layout, state its aspect ratio and preserve its flat/curved geometry, panel boundaries, relative text sizes and placement; do not replace a flat reference with a new decorative composition. Put exact requested text in quotes and require verbatim spelling and placement.
Use one output task per independent asset or variant; dependent edits wait for the preceding real result. Native batch children must use their exact admitted prompt and source IDs without augmentation. Do not fabricate unsupported quality, size, mask, output-path or model parameters or silently lower quality. For transparency, request a real transparent background and preserve alpha; a checkerboard picture or an opaque alpha channel is not verified transparency.
Inspect returned imagery with available vision checks before claiming semantic fidelity, especially text, identity and protected regions. File/CAS success alone is not proof that an edit achieved its goal. If a requirement is missed, state it and propose a focused correction; an authorized correction is a separate visible task, never a hidden retry or replay of an unknown request. Preserve successful sibling results.
For preview requests, present the real image. For a named project/file destination, use the existing attachment/resource and workspace file owners to export and read back the real bytes before saying saved. Preserve the original by default and use a sibling version for an edit unless replacement was requested. Do not replace requested raster artwork with a placeholder; existing editable SVG assets and exact data plots should use their native editing/plotting tools.`
