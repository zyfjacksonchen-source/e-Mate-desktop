default_type application/json;
if ($request_method !~ "^(GET|HEAD)$") {
  return 503 '{"detail":"Skill Hub migration is in progress","error":{"code":"network","message":"Skill Hub migration is in progress"}}';
}
