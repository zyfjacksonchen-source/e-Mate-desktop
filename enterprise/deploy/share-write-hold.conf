# Keep this hold during migration/readback; empty only after the old Worker forwards to this owner.
default_type application/json;
if ($request_method !~ "^(GET|HEAD)$") {
  return 503 '{"schema_version":1,"error":{"code":"SHARE_MIGRATION_IN_PROGRESS"}}';
}
