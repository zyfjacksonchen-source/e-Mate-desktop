# e-Mate 2.0.18 execution and acceptance

User-approved scope: close the original task `01a06a78-3914-7483-a761-0297705a492a`, implement the workstreams and user additions below, accept the exact macOS and Windows installers, and publish those same bytes through the Desktop promotion entry point. This document records work, not completion evidence.

## Baseline and ownership

- Integration starts at `87b47114ff30882d2895682e74d02eb6307b9dbc`; the independent integration branch is `feat/2.0.18/integration`.
- Harness stays at 0.1.5 `e841a5c4add3f7e34c3f7efc8742313debf54922`; Desktop reference stays `166c16cfc38c51d32c2316715548c0f8271db517`. Product identity, Node 24.x, pnpm 11.8.0 and Desktop Yarn remain governed by the current target contract.
- The main agent owns shared contracts, integration, native packaging, installed tests, deployment, release and recovery. Up to three execution agents use distinct worktrees and write sets. They return reviewed source commits and narrow checks; they cannot claim candidate, installed or production acceptance.
- The default Agent preset is the native `code` preset (PTC mode); keep its shipped tool-presentation composition and native Session selection semantics.
- Preserve other worktrees, user data, historical receipts and legacy source databases. Application features use existing native services, events, Tools, Jobs, storage and client slots.

## Ordered work

First reconcile every original EM217 work order and user addition against current source and raw evidence. Recheck compact file cards, file icons, the shared composer frame, removal, draft restart, navigation path redaction, native send, image cancellation/recovery, enterprise model compatibility, offline recovery and the Windows/update repairs. Original release work closes against 2.0.18 acceptance, not a renamed 2.0.17 artifact.

1. **Administration and identity.** Select pending users only within the current filter; show counts and freeze IDs/revisions. Reuse per-user PUT with four-way bounded concurrency, synchronous duplicate-submit exclusion, per-user results, success removal, readback for unknown outcomes, and no automatic overwrite after 409. Model and quota edits can preserve their original values independently; approval requires usable models and a positive quota or explicit unlimited. Validate newly granted models without deleting retained disabled grants. Compare model permissions as sets. Establish a unique login owner at the shared credential boundary for registration, creation, legacy upgrade, reset and import. Preserve the canonical login on password reset. Classify historical duplicates without name-based merging or loss of session, usage or audit references.
2. **Computer Use.** Preserve user foreground, cursor, keyboard and clipboard. Reuse the existing subprocess owner for a lazy Windows helper and audited background UIA/targeted input; preserve exact app/window identity, fresh state, cleanup, timeout and cancellation. Optimize measured macOS AX/targeted-input/capture costs. Unsupported background actions fail explicitly, never activate a window as fallback. Browser tasks prefer the existing CDP owner.
3. **Images (2026-09-10 user replacement).** Replace the former e-Mate imagegen plugin, batch child executor and image-specific rules with `dickpy/dsh-imagegen` v1.5.11 at `1f0ec46bdc477835202d9d497744eaa18692b4e0`, adapted only at the pinned 0.1.5 Tool/attachment and enterprise authentication boundaries. Use its generation/edit Tools and host queue. Keep one conversation entry, the existing e-Mate completed-image cards, Gallery and Canvas; do not mount its sidebar tabs or independent studio. Render actual returned image bytes, retain historical images read-only, and remove old Tool execution/retry paths. Preserve zero image/edit confirmation, source isolation, cancellation, partial results and restart without replaying unknown submissions. Retest the replacement in round two. Performance repair begins with differences from the pinned native DSH source, while keeping the existing real 4/5/8-image, latency, success, retention and billing acceptance criteria.
4. **Canvas (revised MVP).** Use fixed Excalidraw 0.18.1 and a native conversation tab next to Gallery, with image arrows, text annotations and image modification. Follow native light/dark themes and system fonts, keep the main UI compact, and preserve existing multi-page/HTML/slide project data. Agent generation/editing uses existing tools and exact attachment identities. Atomic persistence, conflict handling, restart/crash recovery, insertion deduplication and portable project export remain required. Save before native tab, session, route and settings navigation; failure retains the editor. Carousel navigation must not overlap the first image's canvas button.
5. **Pet.** Adapt dsh-pet `f501139cfb155fd46717a79bb1c158da064dce15` code, excluding its license-excluded sprites. Use the existing Xiaoxin identity for independently permitted v2 assets, 16 directions and the original 30 office scenes. Default enabled, draggable in-app overlay, 112 CSS px. Derive state only from native structured projections; open real task details. Persist settings, debounce state changes and pause for first response/background/minimized/reduced motion.
6. **Astra.** Add gpt-6-astra with medium reasoning through existing enterprise policy, catalog, Gateway, Profile, probe and audit. Preserve existing defaults and compatible older-client catalogs. Astra fast mode defaults off. Real streaming, tool invocation, model and reasoning-parameter evidence is required.
7. **External connections.** Replace the capability-center jump with the Feishu, DingTalk and Tencent Docs status list next to the composer. Status comes from real provider readiness. Clicking appends a service-specific connection draft without replacing existing text/attachments or sending automatically. After user submission the Agent uses existing connection Skills/plugin management/MCP management and secure authorization. Test cancellation, restart, revocation, reconnect and real read-only invocation.
8. **Skill Hub.** Move API, catalog and immutable ZIP storage to the existing e-Mate server: independent TypeScript service, isolated PostgreSQL schema and content-addressed persistent volume. Preserve v1 wire contracts, owner identity, immutable versions/hashes, tombstones, idempotency and install receipts. New clients use the same-server address; the legacy Worker address forwards only to the one new service. Include the human-readable-title fix. Rehearse backup/restore, pause writes, migrate, verify all rows/objects, switch once and retain no dual writer. Validate cross-account discovery/install/update/uninstall.
9. **Full review.** Cover maintained client, plugin, Desktop and enterprise code through real login/approval/chat/attachment/tool/task/output/connection/update journeys. Record reproduction, cause, affected boundary and a focused regression for each confirmed defect; avoid unrelated refactors.
10. **Office (2026-09-10 user replacement and optional delivery).** Remove the former documents, PDF, spreadsheets and ppt-master implementations, their execution rules, office_read/office_write, Calc and Office Python closure. Keep the full `dream-num/dsh-univer-office` 0.2.14 source at `f3a8845dd4c863072b0ae555cdb6a58076c165e7`, with the fixed 0.1.5 Host/Client adaptation, as a separate normal DSH plugin archive. The user explicitly permits Agent discovery and local on-demand installation instead of app bundling, and confirms Univer Pro authorization as a collaborator. Reuse the native trusted plugin catalog, recoverable `dsh plugin` install and restart; the model supplies no package URL. Keep the unrelated meeting-summary and Lieflat Charts Skills plus a small installation-discovery Skill. Preserve all native draft/approval and session/workspace authorization. Desktop universal packaging no longer includes the optional plugin's native libraries. Those dependencies currently support macOS ARM64 and Windows x64; Intel Office activation remains unsupported until real upstream libraries exist. PDF printing does not establish arbitrary existing-PDF editing.

The user's latest continuation replaces the earlier wait-for-all-round-one trigger: once replacement and performance source work is complete, integrate every applicable isolated repair, run native checks/builds and start round two. Preserve the complete 73-item scope and all installed, update and promotion gates. Old imagegen/PDF/Office executor patches superseded by these replacements are not reintroduced.

10. **Session-link sharing (user addition).** Investigate the currently reported create/open failures through the native Session ZIP, authenticated share transport and public link paths. Move the share business service and archive storage onto the enterprise server for mainland China access, retaining session/owner scope, expiry, revocation, archive identity and existing links where valid. Verify small and attachment-heavy sessions, lost responses, repeated clicks, restart/list recovery, unauthorized access, expiry/revocation and actual mainland opening/download. Do not rely on a Worker-domain alias while archives or serving still depend on an overseas path.
11. **Artifact presentation (user addition).** Align completed file artifacts and historical attachments with the existing upload cards, reusing their file-type icons, thumbnail background and filename/type presentation. Preserve genuine image previews, file identities, open/download actions, missing-file states, long names and keyboard navigation. Verify supported file types in light/dark themes and narrow layouts through the actual components, then the installed candidate.
12. **Latest interaction fixes.** Pet dragging must persist its new position without stale settings readback, and its right-click menu must offer settings, reset and persistent close. Project menus expose native workspace removal with confirmation retaining local files and sessions. Refine the four quick-start cards with compact native typography, theme colors and accessible interaction states while preserving draft-only behavior. Missing Astra requires actual tenant enablement and account grants, followed by renewed login and selector/call verification.
13. **Login gate.** Explicit logout, revoked credentials and signed-out restart return to login. Pending identity verification must not expose an interactive workspace. Lost logout responses trigger authoritative identity readback and cannot retain a stale signed-in screen. Preserve local data and authenticated deep links; new login must expose the actual current policy.
14. **Enterprise knowledge and Xin assistant.** Add the native `/knowledge` product page and scoped enterprise knowledge API at `/ecorex-agent/client/knowledge/v1`, reusing the existing source/Wiki/benchmark and PostgreSQL/pgvector owners. Freeze import manifests with source hashes, versions and permissions. Local native Agent/Jobs compile and automatically publish only mechanically validated, correctly scoped references, with durable checkpoints and source revision checks. Public enterprise knowledge uses e-Mate identity; project/customer knowledge remains subject to Xin permissions, with unresolved sources private to uploaders. Graph, list, edges, counts and caches share permission filtering; factual, model-organized, inferred and conflicting content remain distinct. A native Three.js graph supports up to 500 interactive nodes, reader/evidence panels and complete list/reduced-motion/WebGL fallback. Query acceptance remains benchmark p95<=2s, knowledge p95<=5s, initial graph interaction<=2s and 500-node interaction>=30FPS, excluding model generation.
15. **Xin dual entry and preinstalled Skill.** External connection UI and the Agent use the same Host `mcp_manage action=ensure` for `xin-business-assistant` at `https://mvdcm.ecoremedia.net/business-assistant/mcp`. Reuse native OAuth, Credentials and MCP Loader, verify real `get_capabilities`, isolate tenant/user/service credentials and stop previous-account instances immediately. Coalesce connection/refresh, reject late callbacks after account changes and preserve valid credentials through transient network failures. Ship one managed `xin-assistant` Skill with legacy skill compatibility, without duplicate user-facing entries. Actual authorization consent stays with the user; an authorized request resumes afterward. Exact-object destructive business operations retain their single confirmation. Test restart, upgrade, cancel, revoke, account switch, roles, credential exclusion and real read-only/write receipts.

## Required acceptance

- Narrow owner checks during edits; root, enterprise and Desktop checks after integration. Two actual integrated GUI rounds, with fixes reviewed and rebuilt before the second round.
- Administration: filter scope, mixed policy, repeat click, partial failure, lost response, concurrent administrators, model disable/reorder, actual post-approval login, effective catalog and quota. Isolated PostgreSQL concurrency covers every login-identity writer without duplicate owners or orphan users.
- Computer Use: at least 30 standard repetitions on each native platform while foreground input continues; no misdirected input/cursor movement/requested activation. Warm standard action p95 target <=1 second; retain same-machine before/after data and real window, permission, scaling and display scenarios.
- Images: version-correct EM217-108/502/503 protocols; direct local overhead non-regression; first-visible p95 <=120 seconds, four-image terminal p95 <=240 seconds, five-image <=300 seconds. Effective generated-image success >=95%, duplicate generation/billing zero, successful output retention 100%. Legal terminal rate is not generation success.
- Quality: at least 30 blinded pairs across six classes; paired mean margin >=-0.2 and 95% confidence lower bound >=-0.3, with matching prompt/source/model/quality parameters.
- Integrated journey: generate -> edit -> canvas -> arrange -> export -> restart; pet agrees with native task state; all three external connections authorize and invoke; Skill Hub works across real accounts.
- Sharing: create a real share from the candidate, open it from mainland China, retrieve the unchanged native archive and verify its attachments; list it after restart, revoke it and verify the public URL no longer serves content. Preserve request/receipt correlation and specific failure stages. Source checks do not close this acceptance.

## Deployment and evidence

For the current 2.0.18 continuation, the user selected the existing `win-codex` public SSH connection to the signed-in `LAPTOP-ADQ973JN` Windows host for builds and machine checks. Actual installation and GUI receipts from its interactive session remain required before the Windows and joint promotion gates can close. Do not operate Clash or change the user's local proxy, DNS, TUN, routing or network settings.

Reuse existing authorized deployment/credential boundaries. Verify access, capacity and backups early; the prior failed SSH public-key attempt is not evidence about every other management path. Production user-identity conflicts require a redacted preflight and proven ownership, never speculative merging.

Build macOS locally and Windows on the signed-in `win-codex` host through the Desktop-owned commands. Keep canonical application paths, DSH_HOME, userData, installation-id and test session during 2.0.16 -> 2.0.18 native update. Also test new installation, replacement, normal launch and manual same-path recovery of broken 2.0.17.

Record source, candidate, installed and public evidence separately. Private manifests and both installed receipts must pass the existing common validator. Promotion uses only accepted same-source/same-byte artifacts; initialize historical highest version from actual publication evidence, read back immutable objects and aliases, then activate version last. Read back official version/platform pointers and website completion receipt. Missing evidence stays OPEN. Secrets, raw traces, generated media, screenshots and installers remain outside tracked source.
