#!/usr/bin/env python3
"""detect: inventory the elements in an image and print pixel bounding boxes."""

from pathlib import Path
import sys

sys.path.insert(0, str(Path(__file__).resolve().parents[1]))

from detect import main  # noqa: E402


if __name__ == "__main__":
    main()
